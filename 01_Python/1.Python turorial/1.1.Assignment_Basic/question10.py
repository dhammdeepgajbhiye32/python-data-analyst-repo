# Get the substring that starts 4 characters from the end to the last character: for my_string = "Classification", slice the
# the string from the 4th character from the end to the last character.

my_string = "Classification"

print(my_string[-4:])